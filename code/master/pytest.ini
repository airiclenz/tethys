[pytest]
# Run from code/master/. The core modules use flat imports (they run with the
# systemd WorkingDirectory set to core/), so put core/ on the import path.
pythonpath = core
testpaths = core/tests
